/* The logic behind the code is that if the dayOfWeek is sunday then it should print `Sunday it is` and for other days it should just print `Just a normal day`.
*/

let dayOfWeek = 'Monday';
if (dayOfWeek === 'Sunday') {
    console.log('Sunday it is');
} else {
    console.log('Just a normal day');
}